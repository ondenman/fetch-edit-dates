{
  "name": "fetch_edit_dates",
  "version": "1.0.0",
  "description": "Retrieve ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel dev/app.js --out-file build/app.js",
    "chain": "npm run build && node build/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "sync-request": "^3.0.1"
  }
}
